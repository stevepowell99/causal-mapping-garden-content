---
date: 2025-12-26
---

if "Split opposites" is on, 
the magnetic labels prompt should get twice as many examples (chosen from those exceeding the similarity cutoff) and say:
if you see that there are two subgroups with opposite poles, eg
- Improved import controls
- import controls
- weak import controls
- controls on imports
- lax import controls 
- ....
if so, then provide two labels, not one, e.g. in this case "Strong import controls" and "Weak import controls" or "Presence of import controls" and "Absence of import controls" as most appropriate. Find pairs which are balanced, e.g. not "Import controls" vs "Weak import controls"